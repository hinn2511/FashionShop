var sizeList: string[] = [
    "XS",
    "S",
    "M",
    "L",
    "XL",
    "XXL",
];

export default sizeList;

export interface Color {
    colorName: string;
    colorCode: string;
}

export interface Size {
    sizeName: string;
}